services:
  - type: web
    name: pos-system
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: JWT_SECRET
        generateValue: true
    disk:
      name: pos-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1